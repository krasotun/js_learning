// функцию, принимающую на вход два массива
//  и возвращающую количество общих уникальных значений в обоих массивах.
const mock1 = [1, 3, 2, 2];
const mock2 = [3, 1, 1, 2, 5];

const findUniq = (first, second) => {
    for (const firstEl of first) {
        
    }
};
